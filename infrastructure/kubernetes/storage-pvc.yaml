apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ecoeye-data
  namespace: ecoeye
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  # Docker Desktop uses hostpath storage class by default
  storageClassName: hostpath
