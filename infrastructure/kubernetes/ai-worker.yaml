apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-worker
  namespace: ecoeye
  labels:
    app: ai-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ai-worker
  template:
    metadata:
      labels:
        app: ai-worker
    spec:
      containers:
      - name: ai-worker
        image: docker-ai-worker:latest
        imagePullPolicy: Never
        command: ["python", "src/worker.py"]
        env:
        - name: REDIS_URL
          value: "redis://redis:6379"
        - name: GEMINI_API_KEY
          valueFrom:
            secretKeyRef:
              name: ecoeye-secrets
              key: GEMINI_API_KEY
        - name: SUPABASE_URL
          valueFrom:
            secretKeyRef:
              name: ecoeye-secrets
              key: SUPABASE_URL
        - name: SUPABASE_ANON_KEY
          valueFrom:
            secretKeyRef:
              name: ecoeye-secrets
              key: SUPABASE_ANON_KEY
        volumeMounts:
        - name: data
          mountPath: /data
        resources:
          requests:
            memory: "1Gi"
            cpu: "1000m"
          limits:
            memory: "2Gi"
            cpu: "2000m"
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: ecoeye-data
